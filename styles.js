import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  constainer: {
    flex: 1,
    padding: 10,
  },
  heading: {
    fontWeight: "600",
    fontSize: 24,
    marginVertical: 5,
  },
  subHeading: {
    fontSize: 18,
  },
});

export default styles;
