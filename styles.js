import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  constainer: {
    flex: 1,
    padding: 8,
  },
  heading: {
    fontWeight: "600",
    fontSize: 24,
    marginVertical: 5,
  },
  subHeading: {
    fontSize: 16,
  },
});

export default styles;
